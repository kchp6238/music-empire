fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.13.3
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.6.0
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
python-dotenv==1.0.1

# Postgres driver. Unused for local SQLite dev, but required on the deployed
# instance — Railway sets DATABASE_URL to a postgresql:// URL.
psycopg[binary]==3.2.3
