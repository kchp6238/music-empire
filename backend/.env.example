# Local dev defaults to a SQLite file so the backend runs with zero setup.
# For Postgres (docker-compose, or Railway/Render in prod), set e.g.:
# DATABASE_URL=postgresql+psycopg://music_empire:music_empire@localhost:5432/music_empire
DATABASE_URL=sqlite:///./music_empire.db

JWT_SECRET=change-me-in-production
JWT_EXPIRE_MINUTES=30

CORS_ORIGINS=http://localhost:5173
