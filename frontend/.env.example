# Backend base URL. Leave unset for local dev (defaults to http://localhost:8000).
# On Vercel, set this to the deployed Railway backend URL, e.g.
# VITE_API_URL=https://music-empire-backend-production.up.railway.app
VITE_API_URL=
